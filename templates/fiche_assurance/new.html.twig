{% extends 'base-back.html.twig' %}

{% block body %}
<div style="margin-left:20%">
{{ form_start(form,{'attr': {'novalidate': 'novalidate'}} ) }}
    <h1>Create new Fiche Assurance</h1>

    {{ include('fiche_assurance/_form.html.twig') }}

    <a href="{{ path('app_fiche_assurance_index') }}">back to list</a>
    </div>  

   


{% endblock %}
