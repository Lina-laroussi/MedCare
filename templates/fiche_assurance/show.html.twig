<!DOCTYPE html>

<title>FicheAssurance</title>

{% block body %}
    <h1>FicheAssurance</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ fiche_assurance.id }}</td>
            </tr>
            <tr>
                <th>Num_adherent</th>
                <td>{{ fiche_assurance.numAdherent }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ fiche_assurance.description }}</td>
            </tr>
            <tr>
                <th>Date_creation</th>
                <td>{{ fiche_assurance.dateCreation ? fiche_assurance.dateCreation|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Image_facture</th>
                <td>{{ fiche_assurance.imageFacture }}</td>
            </tr>
            <tr>
                <th>Etat</th>
                <td>{{ fiche_assurance.etat }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_fiche_assurance_index') }}">back to list</a>

    <a href="{{ path('app_fiche_assurance_edit', {'id': fiche_assurance.id}) }}">edit</a>

    {{ include('fiche_assurance/_delete_form.html.twig') }}
{% endblock %}
