<!DOCTYPE html>

<title>Edit FicheMedicale</title>

{% block body %}
    <h1>Edit FicheMedicale</h1>

    {{ include('fiche_medicale/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_fiche_medicale_index') }}">back to list</a>

    {{ include('fiche_medicale/_delete_form.html.twig') }}
{% endblock %}
