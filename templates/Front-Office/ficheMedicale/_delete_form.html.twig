<form method="post" action="{{ path('app_fiche_medicale_delete', {'id': fiche_medicale.id}) }}" 
                    onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ fiche_medicale.id) }}">
    <button class="btn btn-secondary submit-btn">Supprimer</button>
</form>
