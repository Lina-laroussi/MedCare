<form method="post" action="{{ path('app_confirm_remboursemet', {'id': remboursement.id}) }}" onsubmit="return confirm('voulez vous vraiment confirmer cette reclamation?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ remboursement.id) }}">
    <button class="btn btn-primary">confimer</button>
</form>
