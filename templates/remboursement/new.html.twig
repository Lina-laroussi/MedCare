{% extends 'sideBar.html.twig' %}


{% block content %}
<div>
{{ form_start(form,{'attr': {'novalidate': 'novalidate'}} ) }}
    <h1 class="mb-4">Ajouter  Remboursement</h1>

    {{ include('remboursement/_form.html.twig') }}

    <a class="btn btn-primary mt-4" href="{{ path('app_remboursement_index') }}">back to list</a>
{% endblock %}
